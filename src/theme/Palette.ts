import { css } from "styled-components";

export const Palette = css`
  :root {
    // Colors
    --atoll: #0a6c63;
    --silver: #f7f7f7;
    --gallery: #eeeeee;
    --wild-sand: #f5f5f5;
    --wisp: #fceaec;
    --white: #ffffff;
    --black: #000000;

    // Color Palette
    --color-primary: var(--atoll);
    --color-secondary: var(--wild-sand);
    --color-tertiary: var(--gallery);
  }
`;
